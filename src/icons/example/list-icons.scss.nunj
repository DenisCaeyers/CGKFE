<head>
<link rel="stylesheet" type="text/css" href="Munalux.css">
</head>
<body>
TEST
<div class="clearfix">
  {% for glyph in glyphs %}
  <div>
    <span class="MunaluxIcon MunaluxIcon-{{ glyph.name }}"></span><br>
    <span class="name">{{ glyph.name }}</span>
  </div>
  {% endfor %}
</div>
</body>